name: AutoUpdate
on:
  schedule:
    - cron: '0 */4 * * *' 
  workflow_dispatch:      
jobs:
  run-script:
    runs-on: ubuntu-latest # Исправлено здесь
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3
# ... дальше всё без изменений

